services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: payout_optimizer
      POSTGRES_USER: payout_user
      POSTGRES_PASSWORD: payout_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: